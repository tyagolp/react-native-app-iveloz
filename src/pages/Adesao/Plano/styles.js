import styled from 'styled-components/native';

import { colors } from '../../../styles';

export const List = styled.FlatList``;
export const ListLoad = styled.ActivityIndicator.attrs({
  color: colors.primary,
  size: 35,
})``;
